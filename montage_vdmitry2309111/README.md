# v.дмитрий2309111 — монтаж 3 роликов

Release tag: `v.дмитрий2309111` (id 394680418).

## Источники

| файл | asset id | размер | sha256 | длительность |
|---|---|---|---|---|
| video1.mp4 | 583828400 | 79211659 | `2486cb97a23cd42c0fc451d1b4536d03d9975ec93813b8665c4f1aa264092126` | 19.650s |
| video2.mp4 | 583819188 | 92622630 | `919493cf45497e0f85c41d780ba8b42443a8a99c6a7b5a0bf672494b532e2e6d` | 23.426s |
| video3.mp4 | 583812752 | 77363523 | `0066085b05bbb94c1b65bc50f5f6e8139d7bb8eda169afa6a02640e324ffdcf1` | 19.160s |

Все три sha256 сверены с digest в release API — совпадают.

Длительность video2 (23.426с) совпадает с длительностью video1 из батча `v.сергей2209` — совпадение случайное, sha256 у файлов разные, персонажи и комнаты разные, отдельной проверки не потребовало.

## Проверка на дубли (протокол §8)

Герой и комната — presenter «Дмитрий», родительская перспектива, доска с цифрами (Q1-132,7/Q2-163,9/Q3-186,1/Q4-229,7/Итог-712,4), монитор со spreadsheet-графиками, тёмный стеллаж, тёмно-синее поло — совпадает с батчами **dmitry11** и **dmitry1809**. Запущена усиленная проверка:

- sha256 всех трёх новых файлов сверен со всеми 71 историческими hash-значениями по всей ветке — совпадений нет.
- Длительности (19.650 / 23.426 / 19.160с) не совпадают с dmitry1809 (31.960/29.400/31.575с) и dmitry11.
- Полный ASR-транскрипт сверен с обоими прошлыми батчами этой комнаты — новые три сюжета (ошибка, не разобранная сразу, откладывается и остаётся / много терминов и дат за короткое время не поддаётся перечитыванию / сложно самостоятельно проверить соответствие заданий экзамену) не пересекаются ни с одним прежним (альтернативный способ решения не считается повтором ошибки; место в рейтинге цифрой без имён одноклассников; синхронизация заданий ночью по wifi; вечернее время по числу нерешённых заданий; все предметы одним доступом; пробник без драматичной музыки).

**Отдельное наблюдение (не дубль).** Три темы этой партии — текстовый разбор, игры на запоминание, банк ФИПИ — тот же набор из трёх базовых офферов кампании, который систематически повторяется в этой сессии под разными тегами и персонажами. Формулировки здесь полностью свои.

## Палитра

Пиксельный замер фона (R/B ratio, все три кадра): 1.271-1.274 — холодный сет, совпадает с историческим замером этой комнаты (dmitry1809: 1.233, dmitry11≈1.26). `pipeline.py` унаследован от dmitry1809 без переключения (`ACCENT = "&H00FFF5C9&"`, #C9F5FF).

## ASR-правки

- video1: «самая» → «самое» (согласование среднего рода с «то что» в «у ребёнка перед ЕГЭ самое незаметное»); «шапки» → «шапке».
- video2: «запоминания» → «запоминание» (неверный падеж после предлога «на»).
- video3: слипшиеся токены «фи»+«пи»+«пи» → «ФИПИ»; клэмп переполнения времени последнего слова (19.168→19.14).

`build_words.py` и `asr_batch.py` унаследованы из dmitry1809 без правок кода.

## Громкость (EBU R128, два прохода, TP-цель −2.0)

| видео | pass1 measured | финал (AAC) |
|---|---|---|
| 1 | I=-17.23 TP=-3.16 LRA=1.90 | I=-14.08 LUFS, TP=-1.77 dBTP |
| 2 | I=-17.36 TP=-4.08 LRA=4.70 | I=-14.23 LUFS, TP=-1.85 dBTP |
| 3 | I=-17.37 TP=-3.77 LRA=3.20 | I=-14.29 LUFS, TP=-1.65 dBTP |

Все три в пределах спецификации (§4.5.1): −14 LUFS integrated, TP ≤ −1.0 dBTP — проверено на готовом AAC-файле, с первой попытки.

## QA

Полный чеклист §4.6 пройден для всех трёх видео: хук закрывается до старта первой карточки, карточки не превышают 4с (длинные фразы разбиты на несколько карточек при построении data.py), иконка «ЕГЭ Тренажёр» появляется на оба голосовых упоминания (260-300px, 100% непрозрачность, справа на уровне головы), бегущая подпись непрерывна и не смещается со слота, CTA «ССЫЛКА в шапке профиля» с верной грамматикой во всех трёх. Стоп-кадры (hook/card+icon/CTA) проверены визуально для всех трёх роликов — дефектов не найдено, холодный акцент отрендерился корректно.

## Компрессия

crf23 (дефолт) уложился в лимит ~30MB для всех трёх: 8.4MB / 9.1MB / 8.0MB. Эскалация до crf27 не потребовалась.

## Файлы в этой папке

`pipeline.py`, `data{1,2,3}.py`, `gen_render.py`, `loudnorm.py`, `asr_batch.py`, `build_words.py`, `make_subs.py` — код монтажа. `subs_d2309111_{1,2,3}.ass` — готовые субтитры. `asr{1,2,3}.json`, `words{1,2,3}.json` — распознавание речи. `render{1,2,3}.sh` — сгенерированные ffmpeg-скрипты рендера.
