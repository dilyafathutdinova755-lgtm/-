# v.дмитрий240912 — монтаж 3 роликов

Release tag: `v.дмитрий240912` (id 395802063).

## Источники

| файл | asset id | размер | sha256 | длительность |
|---|---|---|---|---|
| video1.mp4 (Untitled.Video.2409.1_2160p.1.mp4) | 586216430 | 89942880 | `339e93dd80024f124f316d556d4f9094cff834a85d1bf2f9cd5d1582d83355e2` | 22.850s |
| video2.mp4 (Untitled.Video.2409.2_2160p.1.mp4) | 586207901 | 84085583 | `7ca3acb1d90def65ae15408ec20664d574726363366d61594515d65df2cb038f` | 20.866s |
| video3.mp4 (Untitled.Video.2409.3_2160p.1.mp4) | 586199484 | 88516853 | `180acc18e7c6126d67ede7401bd76bfdf774359db5f668ca898f7721cdb2092e` | 22.380s |

Все три sha256 сверены с digest в release API — совпадают. Совпадений длительности с историческими роликами (по всей ветке) не найдено.

## Проверка на дубли (протокол §8)

Герой и комната визуально совпадают с батчами **dmitry11**, **dmitry1809** и **v.дмитрий2309111** (родительская перспектива, доска с цифрами Q1-132,7/Q2-163,9/Q3-186,1/Q4-229,7/Итог-712,4, монитор со spreadsheet-графиками, тёмный стеллаж, тёмно-синее поло). Запущена усиленная проверка:

- sha256 всех трёх новых файлов сверен со всеми 80 историческими hash-значениями по всей ветке — совпадений нет.
- Длительности (22.850 / 20.866 / 22.380с) не совпадают с v.дмитрий2309111 (19.650/23.426/19.160с) и остальными историческими записями этой комнаты.
- Полный ASR-транскрипт сверен с `v.дмитрий2309111` (последний батч этой комнаты): новые три сюжета (ребёнок раз за разом спотыкается об один тип ошибки, а формальный правильный ответ в других заданиях это скрывает / вариант ЕГЭ, который ребёнок находит сам, скачан без года и источника / ребёнку проще перечитать учебник, чем проверить что реально в памяти) не пересекаются дословно ни с одним прежним скриптом этой комнаты.

**Отдельное наблюдение (не дубль).** Три темы этой партии — текстовый разбор ошибок (video1), банк ФИПИ (video2), игры на запоминание (video3) — тот же набор из трёх базовых офферов кампании, который систематически повторяется в этой сессии под разными тегами и персонажами, включая ранее обработанные батчи этой же комнаты (`dmitry11`, `dmitry1809`, `v.дмитрий2209`, `v.дмитрий2309111`). Формулировки здесь полностью свои — родительская перспектива и конкретные сценарии не повторяют прежние скрипты.

## Палитра

Пиксельный замер фона (R/B ratio, все три кадра): 1.302 / 1.296 / 1.295 — холодный сет, совпадает с историческим замером этой комнаты (v.дмитрий2309111: 1.271-1.274, dmitry1809: 1.233). `pipeline.py` унаследован от `v.дмитрий2309111` без переключения (`ACCENT = "&H00FFF5C9&"`, #C9F5FF).

## ASR-правки

Партия распозналась заметно чище обычного — почти без искажённых токенов:

- video1: правок не потребовалось, «шапке профиля» распознано корректно с первого раза.
- video2: «шапки» → «шапке», «профиль» → «профиля» (падежи в фиксированной CTA-фразе).
- video3: «шапки» → «шапке»; клэмп переполнения времени последнего слова (22.40→22.37, унаследованный баг `build_words.py`, уже был исправлен в этой версии скрипта клэмпом `min(last+0.3, seg['end'])`, но здесь ASR всё равно дал финальный токен на 0.02с за пределами реальной длительности видео — клэмпнуто вручную).

`build_words.py` и `asr_batch.py` унаследованы из `v.дмитрий2309111` без правок кода.

## Громкость (EBU R128, два прохода, TP-цель −2.0)

| видео | pass1 measured | финал (AAC) |
|---|---|---|
| 1 | I=-17.78 TP=-3.68 LRA=4.50 | I=-14.22 LUFS, TP=-1.69 dBTP |
| 2 | I=-17.19 TP=-3.74 LRA=4.70 | I=-14.32 LUFS, TP=-1.42 dBTP |
| 3 | I=-17.98 TP=-3.94 LRA=3.90 | I=-14.28 LUFS, TP=-1.81 dBTP |

Все три в пределах спецификации (§4.5.1): −14 LUFS integrated, TP ≤ −1.0 dBTP — проверено на готовом AAC-файле, с первой попытки.

## QA

Полный чеклист §4.6 пройден для всех трёх видео: хук закрывается до старта первой карточки, длинные фразы (первое предложение video1 длиной 7.1с, video2/video3 аналогично) разбиты на несколько карточек ≤4с при построении data.py, иконка «ЕГЭ Тренажёр» появляется на оба голосовых упоминания в каждом ролике (260-300px, 100% непрозрачность, справа на уровне головы), бегущая подпись непрерывна и не смещается со слота, CTA «ССЫЛКА в шапке профиля» с верной грамматикой во всех трёх. Стоп-кадры (hook/card+icon/CTA/эмфасис-зум) проверены визуально для всех трёх роликов — дефектов не найдено, холодный акцент отрендерился корректно.

## Компрессия

crf23 (дефолт) уложился в лимит ~30MB для всех трёх с большим запасом: 8.5MB / 8.3MB / 8.5MB. Эскалация до crf27 не потребовалась.

## Файлы в этой папке

`pipeline.py`, `data{1,2,3}.py`, `gen_render.py`, `loudnorm.py`, `asr_batch.py`, `build_words.py`, `make_subs.py` — код монтажа. `subs_d240912_{1,2,3}.ass` — готовые субтитры. `asr{1,2,3}.json`, `words{1,2,3}.json` — распознавание речи. `render{1,2,3}.sh` — сгенерированные ffmpeg-скрипты рендера.
